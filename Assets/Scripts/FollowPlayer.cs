using UnityEngine;

public class FollowPlayer : MonoBehaviour
{

    //public Transform player; // Transform permite modificar la posicion y escala del objeto
   // public Vector3 offset; //almacena 3 float. X Y Z

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        //transform.position = player.position + offset;// la posicion de la camara sera igual a la del jugador + las 
        //cordenadas dadas desde Unity en X Y Z 
    }
}
